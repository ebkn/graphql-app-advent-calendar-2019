schema:
  - http://localhost:3000/graphql
documents: ["query.graphql"]
generates:
  ./src/generated/graphql.ts:
    plugins:
      - typescript
      - typescript-operations
    config:
      scalars:
        Time: String
    hooks:
      afterOneFileWrite:
        - npx eslint --fix